procedure m(var x : array of integer);
const cc = 1;
Begin
end;
procedure sum(a : integer);
var mok : array [1..10] of integer;
type
    cp = ls;
begin
    y := y - 2;
    while x >= 10 do begin
        x := x + 1;
    end;
end;
var m : double;
Begin
    x := x + 1;
    z := z - z;
    for i := 1 to 10 do
      x := x - 1;
    sum := 2 + 2;
end.