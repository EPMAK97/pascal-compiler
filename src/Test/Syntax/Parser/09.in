 type
    other = integer;
    tor = record
        intNum    : integer;
        doubleNum : double;
        someNum   : other;
        nestedRec : record
           intNum : integer;
           nestedNum : record
              someNum : other;
           end;
        arr : array[1..10] of integer;
        end;
        c : integer;
    end;
 Begin
    for i := 2 + 2 - 4 / 5 to N do
        s := s + i;

    for i := 1 to N do begin
        x := x + 2;
        y := y - 1;
    end;
 end.