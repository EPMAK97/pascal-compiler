program test;

type real = double;

type

    rec = record
        a: Double;
    end;

function f(a, b, c: Double) : rec;
begin
    Result.a := (a + b) / c;
end;

var
    r: rec;

begin
    r := f(1.0, 10.1, 3);
end.